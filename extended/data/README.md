# Input data

- `sample_stream.jsonl`: the runnable synthetic input used by `main.py`. It contains only upstream observations available to CLARA; evaluation labels are deliberately absent.
- `upstream_pipeline_example.jsonl`: an example of the richer upstream record format supplied separately. It is retained as a reference example and is not consumed by the runtime.
